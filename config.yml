# URL de ton Worker Cloudflare (sera prioritairement lue depuis WORKER_BASE_URL secret GitHub)
worker_base_url: "https://TON-WORKER.workers.dev"

# Destinataire du récap global
global_to:
  - "samuel.lengue@ras-interim.fr"

# Emails des agences (export quotidien + récap 01-15 / 16-fin)
agencies:
  GR:
    daily_to:
      - "samuel.lengue@ras-interim.fr"

  SL:
    daily_to:
      - "agence.salaise@domaine.fr"